import 'package:flutter/material.dart';

class DetailsScreen extends StatelessWidget {
  final data;
  final data2;
  final data3;

  DetailsScreen(this.data, this.data2, this.data3);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("adasdas"),
    );
  }
}
